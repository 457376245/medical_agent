# 医疗 Agent Web 应用 MVP：需求与目标定义

## 0) Intake (Requirements & Missing Info)

### User Story
- As a 患者/家属
- I want 在一个 Web 应用里上传检查单图片/PDF，并自动提取和整理关键信息，生成本次检查结果摘要或用药计划草案
- So that 我能持续、统一地管理病历与用药，减少手工整理成本，提升复诊和自我管理效率

### Scope
- In-scope:
  - 用户上传医疗材料（图片：`jpg/png`，文档：`pdf`）
  - OCR/文档解析 + LLM 结构化整理（如检查项目、数值、单位、参考范围、结论、药品与剂量/频次）
  - 生成两类输出：`检查结果摘要`、`用药计划草案`
  - 用户可查看该次记录，并在时间线上管理历史记录（至少支持按就诊/检查批次聚合）
- Out-of-scope:
  - 自动做医学诊断或替代医生结论
  - 处方开具、购药支付、医保结算
  - 医院系统深度对接（HIS/EMR 实时双向同步）
  - 多角色复杂协作（医生端审核工作流）在 MVP 暂不纳入

### Constraints
- Compatibility:
  - 输出结构需版本化（便于后续迭代字段时保持兼容）
  - 不破坏未来接入第三方 EMR/FHIR 的可能性（先定义内部中立数据模型）
- Performance/Concurrency:
  - 单文件解析结果应在可接受时延内返回（建议 MVP 目标：`P95 <= 90s`）
  - 支持基础并发上传与任务排队（避免超时失败）
- Security/Permissions:
  - 医疗数据属于高敏感信息：传输加密、存储加密、最小权限访问、审计日志必需
  - 明确 AI 生成内容仅供参考的免责声明
- No new deps? (yes/no)
  - 待定（建议：`no` 作为默认优先，必要时再引入）
- Data migration allowed? (yes/no)
  - `yes`（建议允许，因后续结构化 schema 大概率会迭代）

### Missing Info (Blocking / Helpful)
- [Blocking] 合规边界：目标市场是中国内地、海外，还是多区域（影响 PIPL/HIPAA/GDPR 要求）
- [Blocking] LLM/OCR 供应策略：公有云 API 还是私有化部署；是否允许数据出境
- [Blocking] 用药计划草案边界：仅整理既有医嘱，还是允许给出建议（涉及医疗风险）
- [Blocking] 用户身份体系：仅手机号/邮箱，还是需实名与家庭成员代管
- [Helpful] MVP 截止时间、优先级（速度优先/准确优先/成本优先）
- [Helpful] 目标文件规模与日活预期（决定存储、队列、并发策略）
- [Helpful] 是否需要多语言（中文优先或中英双语）

## 0.5) 目标定义（MVP）

### 产品目标（What）
- 建立上传 -> 识别 -> 结构化 -> 生成摘要/用药计划草案 -> 可回看管理的闭环
- 让用户在一个入口统一管理分散病历和用药信息
- 形成可扩展的数据底座，为后续提醒、随访、风险预警打基础

### 业务目标（Why）
- 降低用户手工整理成本与信息丢失率
- 提升复诊前信息准备效率与用药执行清晰度
- 通过高频场景验证医疗 Agent 的核心价值与留存潜力

### 可量化成功指标（MVP 验收口径）
- 上传成功率：`>= 98%`
- 解析任务完成率：`>= 95%`
- 端到端处理时延：`P95 <= 90s`
- 结构化关键字段准确率（抽检）：`>= 85%`
- 用户一次上传后成功获得可读摘要比例：`>= 90%`
- 隐私安全：无高危数据泄露事故，关键操作全量审计

### 非目标（避免范围蔓延）
- 不承诺医疗诊断正确性
- 不做医生替代决策系统
- 不在 MVP 内追求全面医院系统集成

## 4) Acceptance Criteria (Verifiable)
- [ ] 已明确 MVP 用户故事、核心场景、输入/输出边界
- [ ] 已定义 In-scope / Out-of-scope，且可用于后续排期裁剪
- [ ] 已给出可量化目标指标（成功率、时延、准确率、安全）
- [ ] 已识别阻塞性信息缺口（合规、模型策略、医疗责任边界）
- [ ] 已确认 AI 内容仅供参考及隐私保护为硬约束
